@using FamilyBudget.Www.App_Helpers
@model FamilyBudget.Www.Areas.Administration.Models.AccountModel

@{
    ViewBag.Title = "Удаление счета";
}

<div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default">
        <div class="panel-heading">
            <b class="page-header">Удалить счет?</b>
        </div>

        @using (Html.BeginForm())
        {
            <div class="panel-body">
                @Html.ValidationSummary(false, "", new {@class = "alert alert-warning alert-dismissable"})
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.Object.ID)
                @Html.HiddenFor(model => model.Object.Currency.Code)
                @Html.HiddenFor(model => model.Object.Name)
                @Html.HiddenFor(model => model.Object.IsMain)

                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.Object.CurrencyID)
                    </dt>
                    <dd>
                        @Html.DisplayFor(model => model.Object.Currency.Code)
                    </dd>
                    <dt>
                        @Html.DisplayNameFor(model => model.Object.Name)
                    </dt>
                    <dd>
                        @Html.DisplayFor(model => model.Object.Name)
                    </dd>
                    <dt>
                        @Html.DisplayNameFor(model => model.Object.IsMain)
                    </dt>
                    <dd>
                        @Html.DisplayFor(model => model.Object.IsMain)
                    </dd>
                </dl>

                <div class="form-group">
                    <div>
                        <button type="submit" class="btn btn-primary">Удалить</button>
                    </div>
                </div>
            </div>
        }

        <div class="panel-footer">
            @{
                string queryParams = "/Administration/Account" + QueryStringParser.DecodeGridParameters(Model.PageIndex, Model.SortField, Model.SortDirection);
                <a id="backToIncomes" href="@queryParams">Назад к списку счетов</a>
            }
        </div>
    </div>
</div>